# Copy to .env and fill in. Loaded automatically by the training scripts via
# python-dotenv. Do NOT commit your real .env (it is gitignored).

# Comet ML experiment tracking. Leave blank to disable Comet logging entirely.
COMET_API_KEY=
COMET_WORKSPACE=
